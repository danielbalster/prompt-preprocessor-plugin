I've created a new github repo, you're in the local clone. check the ../kilo-gdb-plugin/ folder, which is also one of my github projects: it is a gdb plugin written in typescript for the kilo code AI harness and this is published on npmjs.com. I want you to clone that setup for this project, which is a prompt preprocessor, also a kilo typescript plugin. read the prompt-processor.md for details.  

1. get `gh` up and running, instruct me when you need token and how I can create/provide them
2. get the npmjs access also up and running
3. create all necessary files (template is the gdb plugin) for this project
4. commit and push to github
5. create and verify the github action workflow
6. ensure that the package gets published to npmjs as @dbalster/prompt-preprocessor

